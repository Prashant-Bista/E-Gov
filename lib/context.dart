import 'package:flutter/material.dart';

class TextField extends StatelessWidget {
  final heading;
  final hint;
  const TextField({super.key, this.heading, this.hint});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
